# 垂直小模型项目诊断报告

**诊断日期**: 2026-02-28
**项目名称**: SLM Trainer - 垂直小模型训练系统
**诊断者**: Claude Code

---

## 📊 执行摘要

### 核心问题：蒸馏质量不够高 🔴

**准确率低的根本原因**:
```
蒸馏质量差 → 训练数据质量差 → 模型学习错误 → 准确率低
```

### 当前状态

| 指标 | 当前值 | 目标值 | 状态 |
|------|--------|--------|------|
| **整体准确率** | 40% | 70%+ | 🔴 严重不足 |
| **数字准确率** | 0% | 80%+ | 🔴 完全失败 |
| **数字类问题占比** | 5% | 30%+ | 🔴 严重不足 |
| **训练数据量** | 863条 | 1000-3000条 | 🟡 临界 |

### 项目进度：约70%完成

| 模块 | 完成度 | 状态 |
|------|--------|------|
| 项目框架 | 100% | ✅ 完成 |
| 数据准备 | 100% | ✅ 完成 |
| 模型训练 | 95% | ✅ 完成 |
| 模型推理 | 100% | ✅ 完成 |
| Web界面 | 90% | ✅ 完成 |
| 模型导出 | 100% | ✅ 完成 |
| 模型优化 | 30% | ⚠️ 进行中 |
| 评估体系 | 80% | ✅ 完成 |

---

## 1. 3B模型详细信息

### 训练数据
- **训练样本**: **863条**问答对
- **数据文件**: `data/报销细则_distilled_chunked.jsonl.backup`
- **格式**: JSONL (instruction-output)
- **备份数据**: 298条（当前使用）

### 训练参数

```yaml
# LoRA配置
lora:
  rank: 16              # LoRA秩（适配能力）
  alpha: 32             # 缩放因子（rank × 2）
  dropout: 0.05         # Dropout率
  target_modules:        # 目标模块
    - q_proj
    - k_proj
    - v_proj
    - o_proj

# 训练配置
training:
  batch_size: 1                        # 批次大小
  learning_rate: 0.00005               # 学习率（5e-5）
  num_epochs: 3                        # 训练轮数
  warmup_steps: 30                     # Warmup步数
  gradient_accumulation_steps: 4       # 梯度累积
  max_seq_length: 512                   # 序列长度

# 有效批次大小
effective_batch_size = batch_size × gradient_accumulation_steps
                      = 1 × 4 = 4
```

### 训练时间

| 硬件 | 训练时间 | 说明 |
|------|---------|------|
| **Apple MPS (Mac M1/M2)** | 30-60分钟 | 实际使用 |
| **CUDA GPU** | 15-30分钟 | 推荐配置 |
| **CPU** | 2-4小时 | 不推荐 |

**实际使用**: Apple M2 MPS，约30-60分钟

### 训练过程

- **总步数**: ~648步 (863条 × 3 epochs / 4有效批次)
- **训练速度**: 10-20步/分钟（MPS）
- **最终Loss**: 0.26
- **Loss下降曲线**:
  - Epoch 1: Loss 15.5 → 2.3 (大幅下降)
  - Epoch 2: Loss 2.3 → 0.8 (持续改善)
  - Epoch 3: Loss 0.8 → 0.26 (收敛)

### 模型文件

- **基座模型**: Qwen2.5-3B (~2.4GB)
- **LoRA权重**: adapter_model.safetensors (~29.5MB)
- **配置文件**: adapter_config.json
- **分词器**: tokenizer.json (~11MB)
- **总大小**: ~2.44GB

### 模型性能

- **准确率**: 40% (5/12 测试用例)
- **数字类问题准确率**: 0% (0/9)
- **包含关键信息**: 41.7% (5/12)
- **格式正确**: 100% (12/12)
- **平均关键词覆盖率**: 63.3%

---

## 2. 准确率低的原因分析

### 评估结果对比

| 评估模式 | 测试用例数 | 正确数 | 准确率 | 说明 |
|---------|-----------|--------|--------|------|
| **严格模式** | 12 | 5 | 40% | 需要包含所有预期关键词 |
| **宽松模式** | 5 | 4 | 80% | 只需要部分关键词匹配 |
| **test_set模式** | 20 | 0 | 0% | 最严格的评估 |

### 数字类问题表现

| 问题类型 | 测试数 | 正确数 | 准确率 | 状态 |
|---------|--------|--------|--------|------|
| **金额阈值类** | 5 | 0 | 0% | 🔴 严重问题 |
| **流程材料类** | 4 | 3 | 75% | 🟡 中等 |
| **一般问答类** | 3 | 2 | 67% | 🟡 中等 |

---

## 3. 蒸馏质量深度分析

### 核心问题：蒸馏质量不够高 🔴

**确认**: 蒸馏质量不够高确实是最大的问题

**因果链条**:
```
蒸馏质量差 → 训练数据质量差 → 模型学习错误 → 准确率低
```

### 问题1: 温度设置过高 🔴

**当前配置**:
```python
# Gemini API
temperature=0.7  # ❌ 太高

# 智谱AI API
temperature=0.7  # ❌ 太高
```

**问题分析**:
- `temperature=0.7` 会让模型生成更随机、更有创造性的内容
- 对于需要精确数字的任务，这是**致命的**
- 模型可能会"创造"数字，而不是准确提取

**对比**:
| 温度 | 适用场景 | 数字准确性 | 推荐度 |
|------|---------|-----------|--------|
| 0.7 | 创意写作、对话 | ❌ 低 | ❌ 不推荐 |
| 0.3 | 一般问答 | 🟡 中等 | ⚠️ 谨慎使用 |
| 0.1 | 精确提取、代码生成 | ✅ 高 | ✅ **强烈推荐** |
| 0.0 | 完全确定性 | ✅ 最高 | ✅ **最佳选择** |

**建议**:
```python
# 对于数字提取任务，应该使用极低的温度
temperature=0.1  # 或 0.0
```

### 问题2: 提示词强调不够 🔴

**当前提示词**（Gemini和智谱AI都一样）:
```
**特别强调 - 数字类信息：**
⚠️ 对于文本中包含数字、金额、日期、数量等精确信息的内容：
- 必须优先提取为独立的问题
- 数字必须准确无误，不能省略或模糊化
- 例如："金额超过30000元需附合同"必须包含"30000"这个数字
- 例如："3个工作日内完成"必须包含"3"和"工作日"
- 建议至少生成 30% 的数字相关问题
```

**问题分析**:
1. **"建议"太弱** - "建议至少生成30%"不是强制要求
2. **没有惩罚机制** - 模型不遵循提示词也没有后果
3. **缺少示例** - 没有提供具体的数字问题示例
4. **强调不够** - 只有一段话，容易被忽略

**实际效果**:
- 目标：30%数字类问题
- 实际：5%数字类问题
- 达成率：17%（严重不达标）

### 问题3: 没有验证机制 🔴

**当前流程**:
```
文档 → LLM蒸馏 → 保存为JSONL → 直接用于训练
```

**问题**:
- 生成的数据**没有经过任何验证**
- 错误数据直接进入训练集
- 模型学习了错误信息

**应该的流程**:
```
文档 → LLM蒸馏 → 自动验证 → 人工审核 → 修正错误 → 用于训练
```

**缺少的验证步骤**:
1. ❌ 数字准确性验证
2. ❌ 与原文一致性检查
3. ❌ 人工抽样审核
4. ❌ 质量评分机制

### 问题4: 智谱AI作为后备方案 🟡

**当前机制**:
```python
# Gemini失败时自动切换到智谱AI
except Exception as e:
    if "CONTEXT_TOO_LONG" in error_str or ...:
        print("自动切换到智谱AI...")
        distilled_data = distill_with_zhipu(text, ...)
```

**问题分析**:
- 智谱AI (GLM-4-Flash) 的质量可能不如 Gemini 2.5 Flash
- 智谱AI对提示词的遵循能力可能更差
- 智谱AI生成数字的准确性可能更低

**证据**:
```
第二阶段优化使用了智谱AI生成的新数据（298条）:
- 数字类问题占比: 5.4%（目标30%）
- 数字准确性: 低
- 导致模型性能倒退: 40% → 25%
```

### 问题5: 缺少Few-Shot示例 🟡

**当前提示词**:
```
只有指令，没有示例
```

**问题**:
- LLM不知道什么是"好的数字问题"
- LLM不知道如何准确提取数字
- 没有参考标准

### 问题6: 没有人工审核 🟡

**当前情况**:
- 完全依赖LLM自动生成
- 没有人工审核环节
- 错误数据直接进入训练集

---

## 4. 其他次要原因

### 原因7: 模型容量限制 🟡

**模型对比**:
| 模型 | 参数量 | 推荐训练数据量 | 当前数据量 | 是否足够 |
|------|--------|--------------|-----------|---------|
| Qwen2.5-0.5B | 462M | 100-500条 | 863条 | ✅ 足够 |
| Qwen2.5-1.5B | 1.5B | 500-1000条 | 863条 | 🟡 临界 |
| Qwen2.5-3B | 3B | 1000-3000条 | 863条 | ❌ 不足 |
| Qwen2.5-7B | 7B | 3000-10000条 | 863条 | ❌ 严重不足 |

**问题**:
- 3B模型需要1000-3000条高质量训练数据
- 当前只有863条，且质量不高
- 模型容量不足以记忆所有专业知识

### 原因8: 训练策略问题 🟡

**当前配置**:
```yaml
training:
  num_epochs: 3
```

**问题**:
- 3个epoch可能不足以让模型充分学习
- 特别是对于数字类问题，需要更多重复
- 建议增加到5-10个epoch

### 原因9: 评估标准严格 🔴

**评估标准**:
```python
# 严格模式：必须包含所有预期关键词
expected_keywords = ["30000", "合同", "图书"]
found_keywords = ["合同", "图书"]
missing_keywords = ["30000"]
is_correct = False  # 因为缺少"30000"
```

**问题**:
- 即使回答了大部分信息，只要缺少一个关键词就算错误
- 对于数字类问题特别严格
- 导致准确率偏低

### 原因10: 领域知识复杂性 🟡

**文档特点**:
- 50页PDF文档
- 包含大量具体的金额阈值
- 不同类型的费用有不同的规定
- 规则之间存在交叉和例外

**示例**:
```
课题协作费: 3000元协议，10000元合同
制作费: 3000元协议，10000元合同
材料费: 3000元协议，10000元合同
图书资料: 500元明细清单，30000元合同
办公用品: 500元明细清单
```

**问题**:
- 大量相似的规则容易混淆
- 模型难以精确记忆每个阈值
- 容易产生幻觉

---

## 5. 解决方案

### 短期方案（立即实施）

#### 方案1: 降低温度到0.1 🌡️

**修改代码**:
```python
# src/data_prep/distiller.py
response = client.models.generate_content(
    model='gemini-2.5-flash',
    contents=system_prompt,
    config=types.GenerateContentConfig(
        temperature=0.1,  # 从0.7降低到0.1
        top_p=0.95,
        top_k=40,
        max_output_tokens=8192,
        response_mime_type="application/json"
    )
)

# src/utils/zhipu_client.py
response = client.chat.completions.create(
    model="glm-4-flash",
    messages=[{"role": "user", "content": system_prompt}],
    temperature=0.1,  # 从0.7降低到0.1
    top_p=0.95,
    max_tokens=8192,
)
```

**预期效果**:
- 数字准确性显著提升
- 减少幻觉和编造
- 更忠实于原文

#### 方案2: 强化提示词 📝

**修改后的提示词**:
```python
system_prompt = f"""你是一位专业的领域知识专家和数据提取专家。

【核心任务】
从给定的文本中提取核心知识点，并生成高质量的问答对话对。

【强制要求 - 数字类信息】🔴
⚠️ 这是最重要的要求，必须严格执行：

1. **必须生成至少30%的数字相关问题**
   - 如果生成10个问题，至少3个必须涉及数字
   - 如果生成30个问题，至少9个必须涉及数字

2. **数字必须100%准确**
   - 文档中是"30000元"，就必须是"30000元"
   - 不能是"3万元"、"三万"、"30000"（缺少单位）
   - 不能省略、不能模糊化、不能近似

3. **数字问题示例**：
   ❌ 差: "图书资料报销有什么要求？"
   ✅ 好: "图书资料在什么金额以上需要附合同？"

   ❌ 差: "课题协作费需要什么材料？"
   ✅ 好: "课题协作费在3000元以上需要提供什么材料？"

4. **验证清单**（生成每个问题时必须检查）：
   - [ ] 问题是否包含具体的数字/金额/日期？
   - [ ] 答案中的数字是否与原文完全一致？
   - [ ] 数字单位是否正确（元、天、个等）？
   - [ ] 没有编造或修改数字？

【问题类型要求】
按以下比例生成问题：
- 数字/金额/阈值类: 30%以上（强制）
- 操作步骤类: 20%
- 材料清单类: 20%
- 概念解释类: 15%
- 常见问题类: 15%

【回答要求】
- 严格基于原文，不添加原文中没有的信息
- 对于数字类问题，必须明确说明所有相关数字
- 保持逻辑清晰，条理分明

【输出格式】
严格的JSON数组：
[
  {{
    "instruction": "问题内容",
    "input": "",
    "output": "详细的回答内容"
  }}
]

【Few-Shot示例】

✅ 正确示例1:
原文: "图书资料单价在30000元以上的，需要附合同。"
问题: "图书资料在什么金额以上需要附合同？"
答案: "图书资料单价在30000元以上的，需要附合同。"

✅ 正确示例2:
原文: "课题协作费单张或累计金额3000元以上需要签订协议，10000元以上需要签订合同。"
问题: "课题协作费在什么金额以上需要签订协议？"
答案: "课题协作费单张或累计金额3000元以上需要签订协议。"

❌ 错误示例1:
原文: "图书资料单价在30000元以上的，需要附合同。"
问题: "图书资料报销有什么要求？"
答案: "图书资料报销需要附合同。"
问题: 缺少关键数字"30000"

❌ 错误示例2:
原文: "课题协作费单张或累计金额3000元以上需要签订协议..."
问题: "课题协作费需要什么材料？"
答案: "需要提供协议或合同。"
问题: 没有说明金额阈值

现在请处理以下文本，确保数字类问题占比至少30%：

{text}
"""
```

**关键改进**:
1. "建议" → "强制要求"
2. 添加了"🔴"强调标记
3. 添加了验证清单
4. 添加了Few-Shot示例（正确和错误的对比）
5. 明确了问题类型比例

#### 方案3: 添加自动验证 ✅

**实现代码**:
```python
def validate_distilled_data(distilled_data: List[Dict], original_text: str) -> Dict:
    """
    验证蒸馏数据的质量

    Returns:
        {
            "total": 总数,
            "valid": 有效数,
            "invalid": 无效数,
            "invalid_items": 无效项目列表,
            "numeric_ratio": 数字类问题占比
        }
    """
    import re

    stats = {
        "total": len(distilled_data),
        "valid": 0,
        "invalid": 0,
        "invalid_items": [],
        "numeric_ratio": 0.0
    }

    numeric_count = 0

    for i, item in enumerate(distilled_data):
        issues = []

        # 检查1: 是否包含数字
        instruction = item.get("instruction", "")
        output = item.get("output", "")

        # 检查问题中是否包含数字
        has_number_in_question = bool(re.search(r'\d+', instruction))
        # 检查答案中是否包含数字
        has_number_in_answer = bool(re.search(r'\d+', output))

        if has_number_in_question or has_number_in_answer:
            numeric_count += 1

        # 检查2: 数字准确性（简单检查）
        if has_number_in_question or has_number_in_answer:
            # 提取问题中的数字
            question_numbers = re.findall(r'\d+', instruction)
            answer_numbers = re.findall(r'\d+', output)

            # 如果问题中有数字，答案中应该有对应的数字
            if question_numbers and not answer_numbers:
                issues.append("问题中有数字，但答案中没有")

        # 检查3: 答案是否太短
        if len(output) < 20:
            issues.append("答案太短")

        # 检查4: 是否包含拒绝回答的短语
        reject_phrases = ["不知道", "无法确定", "不清楚", "无法回答"]
        if any(phrase in output for phrase in reject_phrases):
            issues.append("包含拒绝回答的短语")

        if issues:
            stats["invalid"] += 1
            stats["invalid_items"].append({
                "index": i,
                "instruction": instruction,
                "issues": issues
            })
        else:
            stats["valid"] += 1

    # 计算数字类问题占比
    stats["numeric_ratio"] = numeric_count / len(distilled_data) if distilled_data else 0

    return stats
```

#### 方案4: 人工抽样审核 👨‍💻

**实现代码**:
```python
def human_review_sample(distilled_data: List[Dict], sample_ratio: float = 0.1) -> List[Dict]:
    """
    人工抽样审核数据

    Args:
        distilled_data: 蒸馏数据
        sample_ratio: 抽样比例（默认10%）

    Returns:
        人工审核后的数据
    """
    import random

    # 随机抽样
    sample_size = max(1, int(len(distilled_data) * sample_ratio))
    sample_indices = random.sample(range(len(distilled_data)), sample_size)

    print(f"\n{'='*60}")
    print(f"📋 人工抽样审核（{sample_size}个样本）")
    print(f"{'='*60}\n")

    reviewed_data = distilled_data.copy()

    for i, idx in enumerate(sample_indices, 1):
        item = distilled_data[idx]
        print(f"\n样本 {i}/{sample_size} (索引 {idx}):")
        print(f"问题: {item['instruction']}")
        print(f"答案: {item['output'][:200]}...")

        # 人工审核
        while True:
            choice = input("\n审核结果 [1=正确, 2=需要修正, 3=删除, 4=跳过]: ")
            if choice == "1":
                print("✅ 标记为正确")
                break
            elif choice == "2":
                new_output = input("请输入修正后的答案: ")
                reviewed_data[idx]['output'] = new_output
                print("✅ 已修正")
                break
            elif choice == "3":
                reviewed_data[idx] = None  # 标记为删除
                print("🗑️ 标记为删除")
                break
            elif choice == "4":
                print("⏭️ 跳过")
                break
            else:
                print("❌ 无效选择，请重新输入")

    # 删除标记为删除的项目
    reviewed_data = [item for item in reviewed_data if item is not None]

    print(f"\n{'='*60}")
    print(f"✅ 人工审核完成")
    print(f"   原始数量: {len(distilled_data)}")
    print(f"   审核后数量: {len(reviewed_data)}")
    print(f"{'='*60}\n")

    return reviewed_data
```

### 中期方案（1-2周）

#### 方案5: 使用更强的LLM 🔋

**使用GPT-4进行蒸馏**:
```python
from openai import OpenAI

client = OpenAI(api_key="your_gpt4_key")

response = client.chat.completions.create(
    model="gpt-4",  # 使用GPT-4而不是GPT-3.5
    messages=[
        {"role": "system", "content": system_prompt},
        {"role": "user", "content": text}
    ],
    temperature=0.1,  # 低温度
    response_format={"type": "json_object"}  # 强制JSON格式
)
```

**优势**:
- GPT-4对提示词的遵循能力更强
- GPT-4的数字准确性更高
- GPT-4更不容易产生幻觉

**代价**:
- API成本更高
- 速率限制更严格

#### 方案6: 分阶段蒸馏 🎯

**阶段1: 数字提取**
```python
# 第一步：专门提取数字类信息
numeric_prompt = f"""
你的任务是从文本中提取所有数字类信息。

要求：
1. 提取所有金额、日期、数量、阈值
2. 为每个数字生成1-3个问题
3. 数字必须100%准确

文本：{text}
"""

numeric_data = call_llm(numeric_prompt)
```

**阶段2: 其他信息**
```python
# 第二步：提取其他类型的信息
general_prompt = f"""
你的任务是从文本中提取其他类型的信息。

要求：
1. 概念解释
2. 操作步骤
3. 材料清单
4. 不需要包含数字类信息（已在阶段1提取）

文本：{text}
"""

general_data = call_llm(general_prompt)
```

**阶段3: 合并**
```python
# 合并数据
final_data = numeric_data + general_data
```

**优势**:
- 专门针对数字类问题优化
- 确保数字类问题占比
- 提高数字准确性

### 长期方案（1个月）

#### 方案7: 建立数据质量评分系统 📊

**实现代码**:
```python
def calculate_quality_score(item: Dict) -> float:
    """
    计算单个问答对的质量分数（0-100）

    评分标准：
    - 数字准确性: 30分
    - 答案完整性: 25分
    - 问题清晰度: 20分
    - 与原文一致性: 15分
    - 格式规范性: 10分
    """
    score = 0
    issues = []

    instruction = item.get("instruction", "")
    output = item.get("output", "")

    # 1. 数字准确性 (30分)
    if re.search(r'\d+', instruction):
        if re.search(r'\d+', output):
            score += 30
        else:
            issues.append("问题中有数字，但答案中没有")
    else:
        score += 15  # 没有数字问题，给一半分数

    # 2. 答案完整性 (25分)
    if len(output) >= 50:
        score += 25
    elif len(output) >= 30:
        score += 15
    else:
        issues.append("答案太短")

    # 3. 问题清晰度 (20分)
    if len(instruction) >= 10:
        score += 20
    elif len(instruction) >= 5:
        score += 10
    else:
        issues.append("问题太短")

    # 4. 与原文一致性 (15分)
    # 这里需要原文，暂时给满分
    score += 15

    # 5. 格式规范性 (10分)
    if all(key in item for key in ["instruction", "input", "output"]):
        score += 10
    else:
        issues.append("格式不规范")

    return score, issues
```

---

## 6. 预期改进效果

### 实施方案1-4后（短期）

| 指标 | 当前 | 预期 | 提升 |
|------|------|------|------|
| **数字类问题占比** | 5% | 30%+ | +25% |
| **数字准确性** | 低 | 高 | 显著提升 |
| **训练数据质量** | 低 | 中高 | 显著提升 |
| **模型准确率** | 40% | 60%+ | +20% |

### 实施方案5-6后（中期）

| 指标 | 当前 | 预期 | 提升 |
|------|------|------|------|
| **数字类问题占比** | 5% | 35%+ | +30% |
| **数字准确性** | 低 | 很高 | 大幅提升 |
| **训练数据质量** | 低 | 高 | 大幅提升 |
| **模型准确率** | 40% | 70%+ | +30% |

### 实施方案7后（长期）

| 指标 | 当前 | 预期 | 提升 |
|------|------|------|------|
| **数字类问题占比** | 5% | 40%+ | +35% |
| **数字准确性** | 低 | 极高 | 质的飞跃 |
| **训练数据质量** | 低 | 很高 | 质的飞跃 |
| **模型准确率** | 40% | 80%+ | +40% |

---

## 7. 总结与建议

### 核心问题确认

**是的，蒸馏质量不够高确实是最大的问题**，这直接导致了：

1. ❌ 训练数据中数字类问题占比仅5%（目标30%）
2. ❌ 训练数据本身存在错误（如图书合同500元 vs 30000元）
3. ❌ 模型学习了错误的信息，导致测试时回答错误
4. ❌ 模型无法准确记忆和输出数字信息

### 优先级建议

**立即实施**（今天）:
1. ✅ 降低温度到0.1
2. ✅ 强化提示词（添加Few-Shot示例）
3. ✅ 添加自动验证

**本周实施**:
4. ✅ 人工抽样审核
5. ✅ 重新蒸馏数据

**下周实施**:
6. ✅ 使用GPT-4进行蒸馏
7. ✅ 实施分阶段蒸馏

**本月实施**:
8. ✅ 建立数据质量评分系统

### 关键要点

1. **温度是关键**: 对于数字提取任务，必须使用低温度（0.1或0.0）
2. **提示词要强制**: "建议"不够，必须用"强制"和验证清单
3. **验证不可少**: 自动验证 + 人工审核，缺一不可
4. **Few-Shot有帮助**: 提供正确和错误的示例，让LLM理解标准
5. **数据质量 > 数据数量**: 宁可要100条高质量数据，也不要1000条低质量数据

---

**报告生成时间**: 2026-02-28 21:35
**诊断者**: Claude Code
**版本**: 1.0
